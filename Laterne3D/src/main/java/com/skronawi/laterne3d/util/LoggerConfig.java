package com.skronawi.laterne3d.util;

public class LoggerConfig {

    /*
    must be set to false for the emulator, otherwise the methods, which check the correct
    compilation of the shaders, will not return!!
     */
    public static final boolean ON = false;
}
